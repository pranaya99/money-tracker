# Set to 'false' for instant local demo without Plaid.
PLAID_ENABLED=false

# If you later add real Plaid sandbox keys, set PLAID_ENABLED=true and fill below.
PLAID_CLIENT_ID=your_client_id
PLAID_SECRET=your_secret
PLAID_ENV=sandbox

# API server config
API_PORT=8080

# CORS
ALLOWED_ORIGIN=http://localhost:3000
